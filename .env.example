DB_HOST=mysql
DB_NAME=tracker
DB_USER=user
DB_PASSWORD=111111

# JWT Secret Key
# Generate a secure random key for production:
# php -r "echo bin2hex(random_bytes(32));"
# Or use: composer run generate-jwt-key
JWT_SECRET=your-secret-key-change-in-production